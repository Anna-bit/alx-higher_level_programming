#!/usr/bin/node

const argsLength0 = process.argv.Length;
const argsLength = process.argv;
const argsLength1 = process.argv[0];

if (argsLength0 === 2) { 
console.log ('No argument');
}else if (argsLength) {
console.log ('Argument found');
}else {
}console.log ('Arguments found');
